# -*- coding: utf-8 -*-
"""House Price Predicton

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1xGbVRbAH5LBhgbAF64q1W6tlJzz6CowR
"""





# Commented out IPython magic to ensure Python compatibility.
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns
# %matplotlib inline

df = pd.read_csv("/train.csv")

df.head(10)

del df["Id"]

df

df.isnull().sum()

obj = df.isnull().sum()

for key, value in obj.iteritems():
    print( key, ":", value)

missing = df.isnull().sum()

missing = missing[missing>0]

missing

missing.sort_values(inplace = True)

plt.figure(figsize=(10,5))
missing.plot.bar()

del df["PoolQC"]

del df["Fence"]

del df["MiscFeature"]

del df["Alley"]

del df["FireplaceQu"]

plt.figure(figsize=(11,6))
sns.distplot(df["SalePrice"],bins=20)

df["SalePrice"].mean()

df["SalePrice"].median()

num_data = df.select_dtypes(include=[np.number])

cat_data = df.select_dtypes(include=[np.object])

num_data.columns

cat_data.columns

cor = num_data.corr()

plt.figure(figsize=(14,22))
sns.heatmap(cor)

sorted1=cor["SalePrice"].sort_values(ascending = False)

sorted1

plt.figure(figsize=(10,7))
sns.scatterplot(x="GarageCars", y = "SalePrice", data=df)

sns.regplot(x="GarageCars", y = "SalePrice", data = df, scatter = True)

sns.regplot(x="OverallQual", y = "SalePrice", data = df, scatter = True)

plt.figure(figsize=(10,7))
sns.boxplot(x=df["OverallQual"])

import pandas_profiling
report= pandas_profiling.ProfileReport(cat_data) 
report

pip install -U pandas-profiling

pip install pandas-profiling

import pandas_profiling

report=pandas_profiling.ProfileReport(cat_data)
report